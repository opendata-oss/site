---
import Base from "../../layouts/Base.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<Base
  title="Blog | OpenData"
  description="Updates and insights from the OpenData project"
  ogImage="/images/opengraph.png"
>
  <div
    class="bg-white min-h-screen flex flex-col items-center gap-6 px-4 pt-12 pb-12"
  >
    <article class="font-mono text-[#444] max-w-2xl w-full space-y-6">
      <div class="flex items-center justify-between gap-4">
        <h1 class="text-2xl text-[#1a1a1a] font-bold tracking-[-0.025em]">Blog</h1>
        <a
          href="/rss.xml"
          class="text-[#9a9a9a] hover:text-[#4971ab] transition-colors"
          aria-label="Subscribe via RSS"
          title="Subscribe via RSS"
        >
          <svg
            class="w-5 h-5"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="currentColor"
            aria-hidden="true"
          >
            <path d="M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1Z" />
          </svg>
        </a>
      </div>

      {posts.length === 0 && <p class="text-[#9a9a9a]">No posts yet.</p>}

      <ul class="space-y-2">
        {posts.map((post) => (
          <li>
            <a
              href={`/blog/${post.id}`}
              class="flex items-baseline justify-between gap-4 group"
            >
              <span class="text-[#1a1a1a] group-hover:text-[#4971ab] transition-colors">
                {post.data.title}
              </span>
              <time class="text-sm text-[#9a9a9a] shrink-0 tabular-nums">
                {post.data.date.toISOString().slice(0, 10)}
              </time>
            </a>
          </li>
        ))}
      </ul>
    </article>
    <a
      href="/"
      class="font-mono text-sm text-[#6a6a6a] hover:text-[#4971ab] underline underline-offset-4 transition-colors"
    >
      &larr; back home
    </a>
  </div>
</Base>
